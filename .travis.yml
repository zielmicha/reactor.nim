language: c
sudo: required
services:
  - docker
install:
  - docker pull nimlang/nim:0.19.0-ubuntu
script:
  - docker run -v $PWD:/root nimlang/nim:0.19.0-ubuntu bash -c 'set -e; apt-get update; apt-get install -y python3; cd /root; nimble install -y; ./bin/runtests.py'
